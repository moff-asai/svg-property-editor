# SVG Property Editor — 仕様 / 0環境からの起動方法

SVG をアップロード／生成し、要素のプロパティを編集して自動保存し、**SVG・MP4** で書き出せるエディタ。
「アイデンティティ生成」（`dynamic-identity-generator` 由来のコンテンツ）も内蔵。
**Next.js 16（Turbopack）+ Supabase + Cloudflare Workers（OpenNext）** 構成。

---

## 1. 前提環境

| 必要なもの | 用途 | 備考 |
|---|---|---|
| Node.js 20.6+（推奨 22+） | ビルド/実行 | `--env-file-if-exists` を使用 |
| Docker Desktop | ローカル Supabase | `supabase start` が Docker を要求 |
| Supabase CLI | ローカルDB/認証/ストレージ | `npx supabase` で可（インストール不要） |
| （デプロイ時）Cloudflare アカウント / `wrangler` | 本番デプロイ | ローカル起動には不要 |

ブラウザは **Chrome / Edge / Safari 16.4+**（MP4 書き出しに WebCodecs を使用）。

---

## 2. 0環境からの起動手順

```bash
# 1) 取得
git clone <this-repo> svg-property-editor
cd svg-property-editor

# 2) 依存インストール
npm install

# 3) ローカル Supabase を起動（Docker Desktop を起動しておく）
npx supabase start
#   → API: http://127.0.0.1:54321 / Studio: http://127.0.0.1:54323
#   → DB : postgresql://postgres:postgres@127.0.0.1:54322/postgres
#   マイグレーション(documents / generators / svgs バケット)は自動適用される

# 4) 環境変数ファイルを用意（.dev.vars のローカル値をそのまま使う）
cp .dev.vars .env.local
#   .env.local … next のクライアント/Edge が読む（ローカル開発用）
#   .dev.vars  … wrangler(preview/deploy)のビルド/実行が読む
#   どちらも gitignore 済み。値はローカル Supabase の固定値。

# 5) 起動（本アプリは next dev ではなく preview を使う）
npm run preview
#   → build:cf でビルド後、wrangler が http://localhost:8787 で配信
```

ブラウザで **http://localhost:8787** を開き、`/login` からメール＋パスワードでサインアップ
（ローカルはメール確認不要＝即ログイン）。

> **なぜ `next dev` でなく `npm run preview` か**
> Turbopack の `next dev` はこの構成で Edge ミドルウェアに `NEXT_PUBLIC_*` を inline せず全ルート 500 になる。
> `preview`（`opennextjs-cloudflare`）は本番と同じ worker を workerd で動かし、`.dev.vars` を
> `process.env` に注入するためこの問題が起きない。

---

## 3. 主要機能

- **ダッシュボード**（`/`）: 自分の SVG 一覧、アップロード、`生成` への導線、保存した生成物一覧
- **アップロード → エディタ**（`/editor/{id}`）: 要素をクリック選択 → プロパティ編集（塗り/線/線幅/不透明度/移動/拡大縮小/回転/アニメーション）→ **自動保存**
- **書き出し**: `SVGを書き出し`（ベクター）・`MP4を書き出し`（H.264 / WebCodecs + mp4-muxer）
- **生成**（`/generate`）: 2コンテンツ（すべてモード切替）
  - **02 XYZ / 3D**: `XYZ ライン` / `DATA CUBE` / `GRID CUBE`。3Dは面取り四角形の中にクリップ表示
  - **05 LIQUID GLASS / HEX HALO**: ドット場を切替
  - 出力: **SVG / MP4 / PNG**、設定は**ダッシュボードに保存**して再編集可
  - **背景透過**が初期ON

---

## 4. 書き出し仕様（重要）

- **SVG**
  - XYZ / HEX HALO / LIQUID GLASS … **編集可能なベクター**（Illustrator で図形単位で編集可）
  - DATA CUBE / GRID CUBE（3D）… **現在フレームを埋め込んだ静止SVG**（ベクター化不可のため）
  - **背景透過**時は背景 `<rect>` を出さない（透明）
- **MP4** … H.264。**アルファ非対応**のため、背景透過時は**黒背景**になる
- **PNG** … 背景透過を反映（アルファ保持）

---

## 5. デプロイ（Cloudflare Workers / OpenNext）

```bash
npm run deploy   # build:cf → opennextjs-cloudflare deploy
```

- **本番の `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` は実環境変数で渡す**こと。
  `build:cf` は `node --env-file-if-exists=.dev.vars ...` で、ローカルは `.dev.vars`、CI/本番は環境変数が優先される
  （`NEXT_PUBLIC_*` は `.env*` ファイルからはバンドルに inline されず、実環境変数のときのみ inline される）。
- `wrangler.jsonc` / `open-next.config.ts` / `public/_headers` がデプロイ構成。

---

## 6. 構成メモ

- `src/app` … App Router（`/`, `/login`, `/editor/[id]`, `/generate`, `/generate/[content]`, `/auth/callback`）
- `src/lib/svg` … SVG の正規化 / 編集適用 / 直列化 / MP4書き出し / ドキュメント作成
- `src/lib/identity` … 生成コンテンツ（engine / xyzLine / hexHalo / liquidGlass / gridCube / dataCube / structure02 / hexLiquid / registry）
- `src/lib/media/h264.ts` … WebCodecs 共通ヘルパ（SVG/canvas 両書き出しで共有）
- `supabase/migrations` … `documents` / `generators` テーブル + `svgs` バケット（RLS: 所有者のみ）
- `npm run lint` / `npx tsc --noEmit` … 静的検査

---

## 7. トラブルシュート

- **全ルート 500 / `Your project's URL and Key are required`**: `.env.local` 未作成、または `next dev` を使っている。手順4/5を確認。
- **ブラウザで画面が真っ白**: 古いビルドの immutable キャッシュ。ハードリロード、または別ポートで確認。
- **`supabase start` が失敗**: Docker Desktop が起動しているか確認。初回はイメージ取得に時間がかかる。
- **MP4 書き出しが出ない**: WebCodecs 非対応ブラウザ。Chrome/Edge/Safari 16.4+ を使用。
