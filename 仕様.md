# SVG Property Editor — 仕様 / 0環境からの起動方法

SVG をアップロード／生成し、要素のプロパティを編集して自動保存し、**SVG・MP4** で書き出せるエディタ。
「アイデンティティ生成」（`dynamic-identity-generator` 由来のコンテンツ）も内蔵。
**Next.js 16（Turbopack）+ Supabase + Cloudflare Workers（OpenNext）** 構成。

---

## 0. クイックスタート（環境0・コピペのみ / macOS）

**開発ツールが何も入っていない Mac** でも、プロジェクトフォルダの中で次の1行を貼るだけで起動できる。
`setup.sh` が Homebrew / Node / Docker の導入 → Docker起動 → `npm install` → ローカルSupabase起動 →
環境変数作成 → サーバー起動（`http://localhost:8787` を自動で開く）まで一括で行う（何度でも再実行可・冪等）。

```bash
bash setup.sh
```

> **事前に必要なのはプロジェクトフォルダの入手だけ**
> - フォルダを**コピーでもらった**場合 … そのフォルダ内で `bash setup.sh` を実行するだけ。
> - **クローンから始める**場合（非公開リポジトリ＝要 GitHub 認証）:
>   ```bash
>   gh auth login                                        # 未認証なら（対話）
>   gh repo clone moff-asai/svg-property-editor && cd svg-property-editor
>   bash setup.sh
>   ```
>
> **コピペだけで完結しない箇所（初回のみ・OSの仕様）**
> - **Docker Desktop 初回起動**でライセンス同意やパスワード入力のダイアログが出ることがある（同意すれば以降は自動）。
> - フォルダ入手にクローンを使う場合の `gh auth login`（対話）。ブラウザで認証するだけ。
> - 停止は `Ctrl+C`。再開は再度 `bash setup.sh`。

手順を1つずつ手で実行したい場合は「2. クローン〜0環境からの起動」を参照。

---

## 1. 前提環境

| 必要なもの | 用途 | 備考 |
|---|---|---|
| Node.js 20.6+（推奨 22+） | ビルド/実行 | `--env-file-if-exists` を使用 |
| Docker Desktop | ローカル Supabase | `supabase start` が Docker を要求 |
| Supabase CLI | ローカルDB/認証/ストレージ | `npx supabase` で可（インストール不要） |
| （デプロイ時）Cloudflare アカウント / `wrangler` | 本番デプロイ | ローカル起動には不要 |

ブラウザは **Chrome / Edge / Safari 16.4+**（MP4 書き出しに WebCodecs を使用）。

---

## 2. クローン〜0環境からの起動（変更をプッシュできる状態）

> 非公開リポジトリのため、**書き込み権限のあるアカウントで認証してクローン**する。
> `.env.local` / `.dev.vars` は **gitignore 済み＝リポジトリに含まれない**ので、クローン後に自分で作成する。

```bash
# 1) クローン（push できるよう認証込みで取得。いずれか1つ）
gh repo clone moff-asai/svg-property-editor                         # 推奨: GitHub CLI（gh auth login 済み）
# git clone https://github.com/moff-asai/svg-property-editor.git    # HTTPS（認証情報ヘルパ/PAT）
# git clone git@github.com:moff-asai/svg-property-editor.git        # SSH（SSHキー登録済み）
cd svg-property-editor
#   ※ clone すると origin=このリポ が設定され、そのまま push できる状態になる。
#     push には moff-asai/svg-property-editor への write 権限（オーナー/コラボレータ）が必要。

# 2) 依存インストール
npm install

# 3) ローカル Supabase を起動（Docker Desktop を起動しておく）
npx supabase start
#   → API: http://127.0.0.1:54321 / Studio: http://127.0.0.1:54323 / DB: 127.0.0.1:54322
#   マイグレーション(documents / generators / svgs バケット)は自動適用される
#   値の確認: npx supabase status → API_URL / PUBLISHABLE_KEY

# 4) 環境変数ファイルを作成（リポジトリに含まれないので毎回作る。値は supabase status から取得）
PUB_KEY=$(npx --yes supabase status | grep -oE 'sb_publishable_[A-Za-z0-9_-]+' | head -1)
cat > .dev.vars <<EOF
NEXTJS_ENV=development
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=$PUB_KEY
EOF
cp .dev.vars .env.local
#   .env.local … next(クライアント/Edge)用 / .dev.vars … wrangler(preview/deploy)用
#   キーは config.toml の JWT_SECRET から決まるローカル値。手打ちせず supabase status から取得する。

# 5) 起動（本アプリは next dev ではなく preview を使う）
npm run preview
#   → build:cf でビルド後、wrangler が http://localhost:8787 で配信

# 6) 変更を反映（コミット＆プッシュ）
git add -A
git commit -m "変更内容"
git push          # clone 済みなら origin/main を追跡しているのでこれだけでOK
```

ブラウザで **http://localhost:8787** を開き、`/login` からメール＋パスワードでサインアップ
（ローカルはメール確認不要＝即ログイン）。

> **なぜ `next dev` でなく `npm run preview` か**
> Turbopack の `next dev` はこの構成で Edge ミドルウェアに `NEXT_PUBLIC_*` を inline せず全ルート 500 になる。
> `preview`（`opennextjs-cloudflare`）は本番と同じ worker を workerd で動かし、`.dev.vars` を
> `process.env` に注入するためこの問題が起きない。

---

## 2.5 変更の取り込み（保存データを消さずに更新）

**別マシンでクローンした人が保存データ（アカウント / SVG / ジェネレーター設定）を作った後でも、
こちらのコード変更を取り込むとデータは残る。** 理由: データは git ではなく Supabase（Postgres＋Storage、
ローカルは Docker ボリューム）にあり、`git pull` / `git merge` はコードだけを更新して DB を触らないため。

```bash
# 1) コードを取り込む（DB・保存データには一切触れない）
git pull            # または git merge

# 2) 新しいDBマイグレーションがあれば「差分だけ」当てる（既存データは保持）
npm run db:update   # = supabase migration up
#   setup.sh を再実行しても同じ（start → migration up の順で自動実行、reset はしない）
```

- **絶対にやらないこと: `npx supabase db reset`** … ローカルDBを初期化し全データを消去する。更新時は必ず `db:update`（= `migration up`）を使う。
- 追加するマイグレーションは**追加系のみ**（`CREATE` 等）。既存の列/テーブルを消す `DROP` などは書かない（＝取り込んでもデータが消えない）。
- **バックアップ**: 念のためは `npm run db:backup`（`supabase/backup.sql` にデータをダンプ。gitignore 済み＝コミットされない）。復元は `psql ... < supabase/backup.sql`。
- 注意: **ローカルデータはマシンごと**。クローンしてもデータは移動しない。複数マシン/本番で共有・永続したい場合は**クラウド版 Supabase** に接続する（その場合もコード更新でデータは消えない）。

---

## 3. 主要機能

- **ダッシュボード**（`/`）: 自分の SVG 一覧、アップロード、`生成` への導線、保存した生成物一覧
- **アップロード → エディタ**（`/editor/{id}`）: 要素をクリック選択 → プロパティ編集（塗り/線/線幅/不透明度/移動/拡大縮小/回転/アニメーション）→ **自動保存**
- **書き出し**: `SVGを書き出し`（ベクター）・`MP4を書き出し`（H.264 / WebCodecs + mp4-muxer）
- **生成**（`/generate`）: 2コンテンツ（すべてモード切替）
  - **02 XYZ / 3D**: `XYZ ライン` / `DATA CUBE` / `GRID CUBE`。3Dは面取り四角形の中にクリップ表示
  - **05 LIQUID GLASS / HEX HALO**: ドット場を切替
  - 出力: **SVG / MP4 / PNG**、設定は**ダッシュボードに保存**して再編集可
  - **背景透過**が初期ON

---

## 4. 書き出し仕様（重要）

- **SVG**
  - XYZ / HEX HALO / LIQUID GLASS … **編集可能なベクター**（Illustrator で図形単位で編集可）
  - DATA CUBE / GRID CUBE（3D）… **現在フレームを埋め込んだ静止SVG**（ベクター化不可のため）
  - **背景透過**時は背景 `<rect>` を出さない（透明）
- **MP4** … H.264。**アルファ非対応**のため、背景透過時は**黒背景**になる
- **PNG** … 背景透過を反映（アルファ保持）

---

## 5. デプロイ（Cloudflare Workers / OpenNext）

```bash
npm run deploy   # build:cf → opennextjs-cloudflare deploy
```

- **本番の `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` は実環境変数で渡す**こと。
  `build:cf` は `node --env-file-if-exists=.dev.vars ...` で、ローカルは `.dev.vars`、CI/本番は環境変数が優先される
  （`NEXT_PUBLIC_*` は `.env*` ファイルからはバンドルに inline されず、実環境変数のときのみ inline される）。
- `wrangler.jsonc` / `open-next.config.ts` / `public/_headers` がデプロイ構成。

---

## 6. 構成メモ

- `src/app` … App Router（`/`, `/login`, `/editor/[id]`, `/generate`, `/generate/[content]`, `/auth/callback`）
- `src/lib/svg` … SVG の正規化 / 編集適用 / 直列化 / MP4書き出し / ドキュメント作成
- `src/lib/identity` … 生成コンテンツ（engine / xyzLine / hexHalo / liquidGlass / gridCube / dataCube / structure02 / hexLiquid / registry）
- `src/lib/media/h264.ts` … WebCodecs 共通ヘルパ（SVG/canvas 両書き出しで共有）
- `supabase/migrations` … `documents` / `generators` テーブル + `svgs` バケット（RLS: 所有者のみ）
- `npm run lint` / `npx tsc --noEmit` … 静的検査

---

## 7. トラブルシュート

- **全ルート 500 / `Your project's URL and Key are required`**: `.env.local` 未作成、または `next dev` を使っている。手順4/5を確認。
- **ブラウザで画面が真っ白**: 古いビルドの immutable キャッシュ。ハードリロード、または別ポートで確認。
- **`supabase start` が失敗**: Docker Desktop が起動しているか確認。初回はイメージ取得に時間がかかる。
- **MP4 書き出しが出ない**: WebCodecs 非対応ブラウザ。Chrome/Edge/Safari 16.4+ を使用。

---

## 8. 認証・ログイン情報の保存先

Supabase Auth を使用。保存先は3層。

| 何を | どこに | 形式 |
|---|---|---|
| **アカウント（メール＋パスワード）** | Supabase の Postgres `auth.users`（ローカルは Docker ボリューム内） | パスワードは **bcrypt ハッシュ**（`$2a$10$…`）。平文では保存されない |
| **セッション（サーバ側）** | Postgres `auth.sessions` | セッションレコード |
| **セッション（ブラウザ側）** | Cookie `sb-<ref>-auth-token`（+ PKCE 用 `…-code-verifier`）。ローカルは `sb-127-…` | アクセス/リフレッシュトークン。`@supabase/ssr` がミドルウェアで更新 |

- ローカル環境ではすべて**自分のマシンのローカル Supabase（Docker）内**に保存され、外部には出ない。
- 本番（Supabase クラウドに接続する場合）は同じ構造が Supabase プロジェクト側に保存される。
- 補足: エディタの**下書き**はログイン情報とは別に、ブラウザ `localStorage`（`svged:draft:<docId>`）に保持。
- Cookie はホスト単位（ポート非依存）で共有されるため、`localhost:*` の別ポート間でログイン状態が共有される。
